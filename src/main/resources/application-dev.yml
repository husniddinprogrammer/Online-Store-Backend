spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/online_store_dev
    username: postgres
    password: husniddin
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    husniddin.online_store: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
